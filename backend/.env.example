# Copy this file to .env and replace the placeholders with your own Atlas data.
# Never commit backend/.env because it contains database credentials.
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>/<database>?retryWrites=true&w=majority
PORT=4000
NODE_ENV=development
